SAD Lab5- week 5

Installations 

Django

you want to install first python and visual studio code and then open the command prompt to check the python and pip versions.

then  Django-admin --version check. pip install virtualenvwrapper-win command used to collect the packages then 'pip install Django'

this command uses install Django and check the version of Django. next, create a project folder. 'python manage.py runserver' is a 

command to run the server.open the Vs code to select your create the project folder. then open a new terminal to start 

up the work 'python manage.py startapp project name to run to create a new project. now you set up URLs

and you want to see 'hello world' message in localhost. to write the code then run the server localhost to return the message of hello world. 